name: Keep Render Alive
on:
  schedule:
    - cron: '*/10 * * * *' # Каждые 10 минут
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping server
        run: curl -s https://pro-info-api.onrender.com
